{
  "mcpServers": {
    "git": {
      "command": "uvx",
      "args": ["git-mcp"]
    },
    "mlflow": {
      "command": "uv",
      "args": ["run", "--with", "mlflow[mcp]>=3.5.1", "mlflow", "mcp", "run"],
      "env": {
        "MLFLOW_TRACKING_URI": "file:///tmp/mlflow-mcp"
      }
    }
  }
}
